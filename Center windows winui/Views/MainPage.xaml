<Page
    x:Class="CenterWindow.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:CenterWindow.Models"
    xmlns:conv="using:CenterWindow.Converters"
    xmlns:vm="using:CenterWindow.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=False}"
    mc:Ignorable="d">

    <Page.Resources>
        <conv:NullToBoolConverter x:Key="NullToBool"/>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Encabezados -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Título"    Grid.Column="0" FontWeight="SemiBold"/>
            <TextBlock Text="Handle"    Grid.Column="1" FontWeight="SemiBold"/>
            <TextBlock Text="Posición×Dim" Grid.Column="2" FontWeight="SemiBold"/>
        </Grid>

        <!-- ListView con ItemTemplate en columnas -->
        <ListView
          Grid.Row="1"
          ItemsSource="{Binding Windows}"
          SelectedItem="{Binding SelectedWindow, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Title}" Grid.Column="0" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Text="{Binding Handle}" Grid.Column="1" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding Rect}" Grid.Column="2"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Transparencia y botón -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" Margin="0,12,0,0">
            <TextBlock Text="Transparencia:" VerticalAlignment="Center"/>
            <Slider Minimum="0" Maximum="255" Value="{Binding Transparency}" Width="200"/>
            <TextBlock Text="{Binding Transparency}" VerticalAlignment="Center"/>
            <Button Content="Centrar con Alfa"
              Command="{Binding CenterSelectedWindowCommand}"
              IsEnabled="{Binding SelectedWindow, Converter={StaticResource NullToBool}, Mode=TwoWay}"/>
        </StackPanel>
    </Grid>
</Page>

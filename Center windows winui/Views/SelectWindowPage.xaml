<Page
    x:Name="PageRoot"
    x:Class="CenterWindow.Views.SelectWindowPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodel="using:CenterWindow.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodel:SelectWindowViewModel, IsDesignTimeCreatable=True}"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea" ColumnDefinitions="*" RowDefinitions ="*">

        <ScrollView x:Name="ScrollViewPage" ContentOrientation="Vertical"
            HorizontalContentAlignment="Stretch"  VerticalContentAlignment="Stretch"
            ZoomMode="Disabled" IsTabStop="True"
            VerticalAlignment="Top" HorizontalAlignment="Left"
            HorizontalScrollMode="Auto" HorizontalScrollBarVisibility="Auto"
            VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto"
            Width="{Binding ActualWidth, ElementName=ContentArea}"
            Height="{Binding ActualHeight, ElementName=ContentArea}">

            <StackPanel Orientation="Vertical" Style="{StaticResource BaseStackPanel}">

                <Grid ColumnDefinitions="*" RowDefinitions ="*, *">

                    <StackPanel x:Name="PanelSelect"
                                Grid.Row="0"
                                Orientation="Vertical"
                                Style="{StaticResource ContentStackPanel}">
                        <TextBlock
                            Text="Drag the crosshair to select any window"
                            TextAlignment="Center"/>
                        <!-- Transparent button to capture the click event -->
                        <Button
                            x:Name="SelectWindow"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="0"
                            HorizontalAlignment="Center">
                            <!-- Change image according to CurrentImage -->
                            <Image
                                Source="{Binding CurrentImage}"
                                Width="48"
                                Height="44"
                                Stretch="Uniform"/>
                        </Button>
                    </StackPanel>

                    <StackPanel x:Name="PanelData"
                                Grid.Row="1"
                                Orientation="Vertical"
                                Style="{StaticResource ContentStackPanel}"
                                Margin="0, 12, 0, 0"
                                Spacing="4">
                        <Grid x:Name="WindowProperties" ColumnDefinitions="*, *" RowDefinitions ="*, *" ColumnSpacing="12">
                            <StackPanel x:Name="WindowText" Orientation="Vertical" Grid.Row="0" Grid.Column="0">
                                <TextBlock Text="Window text"/>
                                <TextBlock Text="{Binding WindowTitle}"/>
                            </StackPanel>
                            <StackPanel x:Name="WindowHandle" Orientation="Vertical" Grid.Row="1" Grid.Column="0">
                                <TextBlock Text="Window handle"/>
                                <TextBlock Text="{Binding WindowHandle}"/>
                            </StackPanel>
                            <StackPanel x:Name="WindowClassName" Orientation="Vertical" Grid.Row="0" Grid.Column="1">
                                <TextBlock Text="Window class name"/>
                                <TextBlock Text="{Binding WindowClassName}"/>
                            </StackPanel>
                            <StackPanel x:Name="WindowDimensions" Orientation="Vertical" Grid.Row="1" Grid.Column="1">
                                <TextBlock Text="Window dimensions"/>
                                <TextBlock Text="{Binding WindowDimensions}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollView>
    </Grid>
</Page>

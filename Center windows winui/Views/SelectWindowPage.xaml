<Page
    x:Class="CenterWindow.Views.SelectWindowPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodel="using:CenterWindow.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodel:SelectWindowViewModel, IsDesignTimeCreatable=True}"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea">
        <!-- Botón transparente para capturar el clic -->
        <Button
            x:Name="SelectWindow"
            Background="Transparent"
            BorderThickness="0"
            Padding="0">
            <!-- Imagen que cambia según la propiedad CurrentImage -->
            <Image
                Source="{Binding CurrentImage}"
                Width="200"
                Height="200"
                Stretch="Uniform" />
        </Button>
    </Grid>
</Page>
